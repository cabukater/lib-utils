import { GenerateForDirective } from './generate-for.directive';

describe('GenerateForDirective', () => {
  it('should create an instance', () => {
    const directive = new GenerateForDirective();
    expect(directive).toBeTruthy();
  });
});
